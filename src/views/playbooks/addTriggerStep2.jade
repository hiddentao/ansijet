block vars
  - var pageTitle = 'Add trigger - Step 2'
  - var selectedNav = 'playbooks'

include ../_mixins

extends layout

block subContent1
  h2 Set parameters for new trigger

  +form(form, 'Submit')
